const styles = theme => ({
  root: {
    display: "flex",
    flexDirection: "column",
    margin: theme.spacing(1, 1, 1, 1)
  },
  upload: {
    marginTop: theme.spacing(1)
  }
});
export default styles;
