import { teal } from "@material-ui/core/colors";
const styles = theme => ({
  root: {
    flexGrow: 1
  },
  appBar: {
    backgroundColor: teal[500]
  },
  title: {
    flexGrow: 1
  }
});
export default styles;
